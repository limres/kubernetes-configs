apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: system-tools
spec:
  containers:
    - name: busybox
      image: busybox
      command:
        - sleep
        - "3600"
  restartPolicy: Always
