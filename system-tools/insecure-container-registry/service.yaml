apiVersion: v1
kind: Service
metadata:
  name: container-registry
  namespace: system-tools
  labels:
    app: container-registry
spec:
  ports:
  - port: 5000
    protocol: TCP
  selector:
    app: container-registry
